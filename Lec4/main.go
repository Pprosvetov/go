package main

func main()  {


	/*var logo string
	var price int = 23
	fmt.Scan(&logo)
	var len = utf8.RuneCountInString(logo)
	var priceFinal int = price *  len
	if(priceFinal > 100){
		fmt.Printf("%v р. %v коп.\n", math.Round(float64(priceFinal/100)), math.Round(float64(priceFinal % 100)))
		return
	}
	fmt.Printf("%d коп.\n", priceFinal)
*/

	/*var startX, startY, finishX, finishY uint

	var maxX, maxY uint = 8, 8
	fmt.Scan(&startX, &startY, &finishX, &finishY)

	if finishY > maxY || finishX > maxX {
		fmt.Println("НЕТ")
	}

	var cond1 bool =
	if finishY > startY + 2 || finishX > maxX {
		fmt.Println("НЕТ")
	}*/



	/*var str, str2, str3 string
	fmt.Scan(&str, &str2, &str3)
	var cond bool = (str == "раз" || str == "один") && str2 == "два" && str3 == "три"
	var cond2 bool = str == "1" && str2 == "2" && str3 == "3"

	if cond || cond2 {
		fmt.Println("ОК")
	} else {
		fmt.Println("НЕ ПРАВИЛЬНО")
	}*/

	/*var num1, num2 float64
	fmt.Scan(&num1, &num2)
	var num3 int = int(num1 + num2)
	if num3 % 2 == 0 {
		fmt.Println("ЧЁТНОЕ")
	} else {
		fmt.Println("НЕЧЁТНОЕ")
	}*/

	/*var str string
	fmt.Scan(&str)
	if strings.Contains(str, "чат") {
		fmt.Println("БОТ")
	} else {
		fmt.Println("НЕ БОТ")
	}*/

	/*var str, str2 string
	fmt.Scan(&str, &str2)
	if strings.Contains(str, "@") || len(str) < 10 {
		fmt.Println("Некорректный логин")
	} else if !strings.Contains(str2, "@") || !strings.Contains(str2, ".") {
		fmt.Println("Некорректная почта")
	} else {
		fmt.Println("ОК")
	}*/
}